---
steps:
  - name: gcr.io/google.com/cloudsdktool/cloud-sdk:alpine
    dir: functions/process_incoming_image
    entrypoint: gcloud
    args:
      - functions
      - deploy
      - process_incoming_image
      - --memory=256MB
      - --project=${PROJECT_ID}
      - --region=us-east4
      - --runtime=python39
      - --timeout=30
      - --trigger-resource=${INCOMING_BUCKET}
      - --vpc-connector=broad-vpc-us-east4
      - -q
